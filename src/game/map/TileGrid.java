/*
package game.map;

*/
/**
 * TODO: complete javadoc
 *//*

public class TileGrid
{
    //fields
    public Tile[][] map;

    //constructor
    public TileGrid()
    {
        map = new Tile[8][4];
        for (int i = 0; i < map.length; i++)
        {
            for (int j = 0; j < map[i].length; j++)
            {
                map[i][j] = null;
                //FIXME: null is not true...
            }
        }
    }

    //methods

    */
/**
     * TODO: complete javadoc
     *//*

    public void draw()
    {
        for (int i = 0; i < map.length; i++)
        {
            for (int j = 0; j < map[i].length; j++)
            {
                Tile tmp = map[i][j];
                //TODO: we need s.th to draw the Tiles...e.g: DrawQuadText
                //FIXME: null is not true...
            }
        }
    }
}
*/
